# 源码构建模式覆盖：与 docker-compose.yml 合并使用
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build
services:
  searchhub:
    build:
      context: .
      args:
        SEARCHHUB_COMMIT: ${SEARCHHUB_COMMIT:-dev}
    image: searchhub:local
